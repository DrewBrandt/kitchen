{
  "name": "pantry-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest run",
    "check": "tsc -b --pretty false",
    "db:push": "supabase db push",
    "db:lint": "supabase db lint --linked --schema public --level warning --fail-on error",
    "db:test": "supabase db query --linked --file supabase/tests/database.sql",
    "data:firebase-export": "node tools/export-firestore.mjs",
    "data:firebase-transform": "node tools/transform-firestore-snapshot.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "@testing-library/user-event": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "firebase-tools": "15.28.1",
    "jsdom": "29.0.0",
    "supabase": "2.116.0",
    "vitest": "latest"
  }
}
